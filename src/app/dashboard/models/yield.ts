import { RangeType } from '../../ngx-mat-range-slider/ngx-mat-range-slider.component';

export interface Yield {
  periplasm: Number | RangeType;
  cytoplasm: Number | RangeType;
  other: Number | RangeType;
}
