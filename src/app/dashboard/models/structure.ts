import { RangeType } from '../../ngx-mat-range-slider/ngx-mat-range-slider.component';

export interface Structure {
  pdb: string | string[];
  model: string | string[];
  relatedPDB: string | string[];
}
